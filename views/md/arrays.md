In javascript an array is an object. Below is examples of array containing data:

```javascript
// Empty array
const myList = [];

// Array of numbers
const numberArray = [2, 4, 6, 8];

// Array of strings
const stringArray = ['eat', 'work', 'sleep'];

// Array with mixed data types
const newData = ['work', 'exercise', 1, true];
```